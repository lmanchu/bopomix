# Changelog

混打注音相對於上游小麥注音（McBopomofo）的變更。上游本身的版本紀錄見 [McBopomofo releases](https://github.com/openvanilla/McBopomofo/releases)。

## 0.1.0（未發布）

第一個公開版。基底是 McBopomofo `f5ba010`（2026-09）。

### 新增

**中英混打，不切換輸入法**（`MixedScriptEnabled`，預設開）

- 字母序列一旦拼不出合法注音（例如 `th`，或 `acer` 打到第二個字母），整段轉成英文，組字區直接顯示原始字母；英文後面的空白鍵就是真的空白。
- 拼得出注音、又剛好是英文單字的序列（`app`、`ell`、`all`、`full`）維持中文，英文形式放在候選窗第二列，按一次 Tab 切過去。用 Tab 或候選窗選過的英文詞會寫進個人詞庫，之後同一個詞加空白就直接判成英文（`MixedScriptLatinOnSpaceForUserWords`，預設開）。
- 永不靜默丟字：任何情況下組字區都顯示你打的原始字母。
- 只在標準（大千）配置與「注音」模式下作用；關掉時所有程式路徑短路，行為與上游完全相同（有測試釘住）。
- 數字（維護者自己 200 句中英混打語料，走 app 的真實按鍵路徑）：英文 token 保留率 0% → 90.6%，純中文句子與上游逐字元相同（95.3% 同音字正確率不變）；混打開與關的每句延遲差異約 0.3 ms（`tools/eval/BASELINE.md` 純中文 on／off 對照）。

**英文預測與 Tab 補全**（`LatinCompletionEnabled`，預設開；需要混打也開著）

- 英文打到 4 個字母，組字區下方提示完整的字加 ⇥；Tab 接受（游標停在字尾，可以繼續打），Shift+Tab 開候選窗列出多個，用平常的選字鍵挑；候選窗開著繼續打字母會更新清單，Esc 回到原始字母。
- 4 個字母的門檻是實測來的：用 17 個每天會打的詞共 100 個按鍵測，門檻 2 時螢幕上出現 33 次錯的提示、只省 7 個按鍵；門檻 4 錯誤提示降到 11 次、省下的按鍵只少 1 個。Tab 與 Shift+Tab 從 2 個字母起就能用，只是不主動提示。
- 已經是完整的字、而且常用度不比更長的補全差時（`acer` 相對 `acerbic`），視為打完了，不提示也不補。
- 排序：個人詞庫（用得多的優先）＞ 手工科技詞表 ＞ 基礎詞典（依常用度分級）＞ 字母序。基礎詞典來自 SCOWL/ESDB size ≤ 70 的美式拼法詞表，141,697 個詞、含變化形，分級是 5 個粗桶。
- 數字（同一份語料）：英文 token 在 4 個字母內可補全 23%（有個人詞庫歷史時 28.8%），每個 token 省 0.39 到 0.46 個按鍵。不可補全的 47% 裡，81 個是排序沒排到、55 個不在詞典、18 個還沒被判成英文、32 個低於提示門檻。

**從打過的英文學習**（`LatinLearnTypedWords`，預設開）

- 打完一段純英文並以 Enter、空白或標點提交，就計入個人詞庫，不只 Tab 接受的才算。
- 一次提交加 1 分，2 分才影響排序與建議；詞典裡沒有的字（人名、產品名）第一次以 1 分寫入但不會被建議，第二次提交才會。兩次不必在同一次開機內。
- 只記提交出去的英文，Esc 或 Backspace 取消的不記，注音與中文從不記錄。Shift 強制大寫的路徑（上游既有）也不記。
- 寫入採先讀現有檔再合併、暫存檔改名落盤；換使用者詞庫資料夾時一起重新載入，另一台機器同步回來的 `latin-user.txt` 不會被蓋掉。
- 檔案：`~/Library/Application Support/Bopomix/latin-user.txt`，格式 `詞<TAB>次數`，沒有次數的行視為 1。手動刪掉的字要重啟輸入法才會忘記。

**改名為 Bopomix 混打注音，並自動接手小麥注音的資料**

- Bundle id `io.github.lmanchu.inputmethod.bopomix`（macOS 要求輸入法的 bundle id 含 `inputmethod` 這一段，否則註冊會被靜默忽略）。
- 第一次啟動時把 `org.openvanilla.inputmethod.McBopomofo` 的偏好複製到新身分（只補沒有的鍵；自訂詞庫位置與指向舊 app 的 hook 路徑除外），並把 `~/Library/Application Support/McBopomofo/` 逐行合併到 `Bopomix/`（缺的檔複製，其餘只補目的地沒有的行；`latin-user.txt` 以字為單位合併並保留較大的次數）。舊資料只讀不寫，新資料不覆蓋，可以重試。失敗或自訂位置暫時無法讀取時下次啟動再試，並顯示一次通知。
- 引擎的 C++ 命名空間、`McBopomofoLM` 與所有上游檔頭刻意保留，方便跟上游同步。

### 開發與測試

- 引擎的 headless 評測工具 `bopomix-eval`（cmake），可以不裝輸入法就量測「按鍵序列 → 組句」。語料由 `tools/eval/build_corpus.py` 從你自己的文字產生，不隨 repo 附上。
- XCTest 走真實的 `KeyHandler`，評測數字以 app 路徑為準（CLI 的數字曾經誤導過一輪）。
- 測試不得碰真實的偏好與使用者詞庫資料夾：`tools/eval/check_plist_unchanged.sh` 把整套測試包起來跑，前後比對零差異；測試套件必須序列執行。
- 每一輪實作都有獨立的 fresh-context 審查與複驗記錄，原樣放在 `docs/`。

### 已知問題

- 拼得出注音的英文字（`mac` = ㄇㄚ）第一次一定要按 Tab；`ai`、`api`、`app`、`agent`、`acer`、`chat`、`check`、`draft`、`free` 這類高頻詞都屬此類。
- 補全候選窗前三名之外是字母序（詞典只有 5 個常用度分桶）。下一步是換成真實詞頻。
- 補全候選窗開著時按小寫字母會關窗。
- 英文後連按兩個空白會跳出空白的候選窗。
- 許氏、倚天 26 配置沒有混打。
- 整句 AI 選字（本機小模型重排候選）研究過但未實作。
